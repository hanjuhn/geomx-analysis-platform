import React from "react";
import ReactDOM from "react-dom/client";
import GeoMxWebRApp from "./GeoMxWebRApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GeoMxWebRApp />
  </React.StrictMode>
);